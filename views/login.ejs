<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes, viewport-fit=cover">
    <meta name="description" content="DarkBot - Your AI-powered assistant. Log in to start chatting.">
    <meta name="theme-color" content="#8b5cf6">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Outfit', sans-serif;
            background: #080808;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        /* Mesh Background */
        .mesh-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: 
                radial-gradient(circle at 0% 0%, rgba(139, 92, 246, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 100% 100%, rgba(220, 20, 60, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 50% 50%, #000 0%, #080808 100%);
        }

        .auth-card {
            width: 100%;
            max-width: 420px;
            background: rgba(18, 18, 18, 0.7);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 32px;
            padding: 48px;
            box-shadow: 0 40px 100px rgba(0,0,0,0.8);
            animation: cardFadeIn 0.8s cubic-bezier(0.19, 1, 0.22, 1);
        }

        @keyframes cardFadeIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .auth-logo {
            text-align: center;
            margin-bottom: 40px;
            text-decoration: none;
            display: block;
        }

        .auth-logo img {
            width: 64px;
            height: 64px;
            border-radius: 50%;
            margin-bottom: 16px;
            box-shadow: 0 0 30px rgba(220, 20, 60, 0.4);
            transition: transform 0.6s ease;
        }

        .auth-logo:hover img {
            transform: rotate(360deg);
        }

        .auth-logo h1 {
            font-size: 32px;
            font-weight: 800;
            background: linear-gradient(135deg, #fff 0%, #8b5cf6 100%);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .auth-subtitle {
            text-align: center;
            color: #888;
            font-size: 15px;
            margin-bottom: 32px;
            line-height: 1.5;
        }

        .form-group {
            margin-bottom: 24px;
            position: relative;
        }

        .form-group label {
            display: block;
            font-size: 13px;
            font-weight: 600;
            color: #aaa;
            margin-bottom: 8px;
            margin-left: 4px;
        }

        .toggle-password {
            position: absolute;
            right: 16px;
            bottom: 14px;
            color: #666;
            cursor: pointer;
            transition: color 0.3s ease;
            z-index: 10;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 30px;
            height: 30px;
        }

        .form-group input {
            width: 100%;
            padding: 14px 45px 14px 20px;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 14px;
            color: #fff;
            font-size: 15px;
            transition: all 0.3s ease;
        }

        .auth-btn {
            width: 100%;
            padding: 16px;
            border-radius: 14px;
            background: linear-gradient(135deg, #8b5cf6 0%, #3b82f6 100%);
            color: #fff;
            font-size: 16px;
            font-weight: 700;
            margin-top: 12px;
            box-shadow: 0 10px 25px rgba(139, 92, 246, 0.4);
            transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
        }

        .auth-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 15px 35px rgba(139, 92, 246, 0.5);
        }

        .auth-btn:disabled {
            opacity: 0.7;
            cursor: not-allowed;
            transform: none;
        }

        .auth-footer {
            margin-top: 32px;
            text-align: center;
            font-size: 14px;
            color: #777;
        }

        .auth-footer a {
            color: #8b5cf6;
            font-weight: 700;
            text-decoration: none;
        }

        .auth-error {
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.2);
            color: #ef4444;
            padding: 12px;
            border-radius: 12px;
            font-size: 13px;
            text-align: center;
            margin-bottom: 24px;
            display: none;
        }

        .auth-error.show { display: block; }
    </style>
</head>
<body>
    <div class="mesh-bg"></div>

    <div class="auth-card">
        <a href="/" class="auth-logo">
            <img src="/img/sharingan-logo.svg" alt="DarkBot">
            <h1>DarkBot</h1>
        </a>

        <p class="auth-subtitle">Welcome back. Experience AI like never before.</p>

        <div class="auth-error" id="authError"></div>

        <form id="loginForm">
            <div class="form-group">
                <label for="email">Email Address</label>
                <input type="email" id="email" name="email" placeholder="you@example.com" required autocomplete="email">
            </div>

            <div class="form-group">
                <label for="password">Password</label>
                <div class="password-wrapper" style="position: relative;">
                    <input type="password" id="password" name="password" placeholder="Enter your password" required autocomplete="current-password" style="padding-right: 48px;">
                    <button type="button" class="toggle-password" onclick="togglePassword('password')" aria-label="Toggle password visibility" style="position: absolute; right: 8px; top: 50%; transform: translateY(-50%); background: transparent; border: none; color: #666; cursor: pointer; padding: 8px; z-index: 20; display: flex; align-items: center; justify-content: center;">
                        <i class="fa-solid fa-eye" id="passwordIcon" style="font-size: 16px; pointer-events: none;"></i>
                    </button>
                </div>
            </div>

            <button type="submit" class="auth-btn" id="loginBtn">
                <span id="loginBtnText">Log In</span>
            </button>
        </form>

        <div class="auth-footer">
            New here? <a href="/signup">Create account</a>
        </div>
    </div>

    <script>
        function togglePassword(fieldId) {
            console.log('Login: Toggling password for', fieldId);
            const input = document.getElementById(fieldId);
            const icon = document.getElementById(fieldId + 'Icon');
            
            if (!input || !icon) return;

            if (input.type === 'password') {
                input.type = 'text';
                icon.classList.remove('fa-eye');
                icon.classList.add('fa-eye-slash');
            } else {
                input.type = 'password';
                icon.classList.remove('fa-eye-slash');
                icon.classList.add('fa-eye');
            }
        }

        // Add event listener to handle potential issues with inline onclick
        document.addEventListener('DOMContentLoaded', () => {
            const toggles = document.querySelectorAll('.toggle-password');
            toggles.forEach(toggle => {
                toggle.addEventListener('click', (e) => {
                    console.log('Toggle clicked at DOM level');
                });
            });
        });

        document.getElementById('loginForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const errorEl = document.getElementById('authError');
            const btn = document.getElementById('loginBtn');
            const btnText = document.getElementById('loginBtnText');

            errorEl.classList.remove('show');
            btn.disabled = true;
            btnText.textContent = 'Authenticating...';

            try {
                const res = await fetch('/auth/login', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        email: document.getElementById('email').value,
                        password: document.getElementById('password').value
                    })
                });

                const data = await res.json();

                if (data.success) {
                    window.location.href = '/chat';
                } else {
                    errorEl.textContent = data.message;
                    errorEl.classList.add('show');
                }
            } catch (err) {
                errorEl.textContent = 'Connection error. Please try again.';
                errorEl.classList.add('show');
            } finally {
                btn.disabled = false;
                btnText.textContent = 'Log In';
            }
        });
    </script>
</body>
</html>
